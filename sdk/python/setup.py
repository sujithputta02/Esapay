from setuptools import setup, find_packages

setup(
    name="esapay",
    version="1.0.1",
    description="Official Python SDK for Executable State Architecture (ESA) Autonomous Payment Resilience",
    packages=find_packages(),
    python_requires=">=3.8",
    install_requires=[],
)
